<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crossborder.shop.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.crossborder.shop.entity.SystemConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="description" property="description"/>
        <result column="group_name" property="groupName"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT *
        FROM tb_system_config
        WHERE deleted = 0
        ORDER BY group_name ASC, id ASC
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT *
        FROM tb_system_config
        WHERE config_key = #{configKey} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByKeys" resultMap="BaseResultMap">
        SELECT *
        FROM tb_system_config
        WHERE deleted = 0
          AND config_key IN
          <foreach collection="keys" item="key" open="(" separator="," close=")">
              #{key}
          </foreach>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_system_config (
            config_key, config_value, config_type, description, group_name, status,
            create_time, update_time, create_by, update_by, deleted
        ) VALUES (
            #{configKey}, #{configValue}, #{configType}, #{description}, #{groupName}, #{status},
            NOW(), NOW(), #{createBy}, #{updateBy}, 0
        )
    </insert>

    <update id="updateByKey">
        UPDATE tb_system_config
        <set>
            <if test="configValue != null">config_value = #{configValue},</if>
            <if test="configType != null">config_type = #{configType},</if>
            <if test="description != null">description = #{description},</if>
            <if test="groupName != null">group_name = #{groupName},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE config_key = #{configKey} AND deleted = 0
    </update>

</mapper>
