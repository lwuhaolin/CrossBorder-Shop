<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crossborder.shop.mapper.CartMapper">

    <resultMap id="BaseResultMap" type="com.crossborder.shop.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, create_time, update_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_cart
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_cart (user_id, create_time, update_time)
        VALUES (#{userId}, NOW(), NOW())
    </insert>

    <update id="updateById">
        UPDATE tb_cart
        SET update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="deleteByUserId">
        DELETE FROM tb_cart
        WHERE user_id = #{userId}
    </update>

</mapper>
