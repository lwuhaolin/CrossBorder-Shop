<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crossborder.shop.mapper.LogisticsCompanyMapper">

    <resultMap id="BaseResultMap" type="com.crossborder.shop.entity.LogisticsCompany">
        <id column="id" property="id"/>
        <result column="company_name" property="companyName"/>
        <result column="company_code" property="companyCode"/>
        <result column="contact" property="contact"/>
        <result column="website" property="website"/>
        <result column="logo_url" property="logoUrl"/>
        <result column="status" property="status"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectActiveList" resultMap="BaseResultMap">
        SELECT
        *
        FROM tb_logistics_company
        WHERE status = 1
          AND deleted = 0
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT
        *
        FROM tb_logistics_company
        WHERE company_code = #{companyCode}
          AND deleted = 0
        LIMIT 1
    </select>

</mapper>