<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crossborder.shop.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.crossborder.shop.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_number" property="orderNumber"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_code" property="productCode"/>
        <result column="image_url" property="imageUrl"/>
        <result column="sku_id" property="skuId"/>
        <result column="sku_name" property="skuName"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id , order_id, order_number, product_id, product_name, product_code, image_url, sku_id, sku_name
        ,price, quantity, total_price,
        create_time, update_time, create_by, update_by, deleted
    </sql>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM tb_order_item
        WHERE order_id = #{orderId}
        AND deleted = 0
        ORDER BY create_time ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_order_item
           (order_id, order_number, product_id, product_name, product_code, image_url, sku_id, sku_name,
            price, quantity, total_price, create_time, update_time, create_by, update_by)
        VALUES
           (#{orderId}, #{orderNumber}, #{productId}, #{productName}, #{productCode}, #{imageUrl}, #{skuId}, #{skuName},
            #{price}, #{quantity}, #{totalPrice}, NOW(), NOW(), #{createBy}, #{updateBy})
    </insert>

    <insert id="batchInsert">
        INSERT INTO tb_order_item
        (order_id, order_number, product_id, product_name, product_code, image_url, sku_id, sku_name,
         price, quantity, total_price, create_time, update_time, create_by, update_by)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.orderNumber}, #{item.productId}, #{item.productName}, #{item.productCode},
             #{item.imageUrl}, #{item.skuId}, #{item.skuName}, #{item.price}, #{item.quantity}, #{item.totalPrice},
             NOW(), NOW(), #{item.createBy}, #{item.updateBy})
        </foreach>
    </insert>

</mapper>
