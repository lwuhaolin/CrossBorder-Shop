<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crossborder.shop.mapper.ShippingAddressMapper">

    <resultMap id="BaseResultMap" type="com.crossborder.shop.entity.ShippingAddress">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="country" property="country"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="postal_code" property="postalCode"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>



    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        *
        FROM tb_shipping_address
        WHERE id = #{id}
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        *
        FROM tb_shipping_address
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY is_default DESC, update_time DESC
    </select>

    <select id="selectDefaultByUserId" resultMap="BaseResultMap">
        SELECT
       *
        FROM tb_shipping_address
        WHERE user_id = #{userId}
        AND is_default = 1
        AND deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_shipping_address
        (user_id, receiver_name, receiver_phone, country, province, city, district,
         detail_address, postal_code, is_default, create_time, update_time)
        VALUES
        (#{userId}, #{receiverName}, #{receiverPhone}, #{country}, #{province}, #{city}, #{district},
         #{detailAddress}, #{postalCode}, #{isDefault}, NOW(), NOW())
    </insert>

    <update id="updateById">
        UPDATE tb_shipping_address
        SET receiver_name = #{receiverName},
            receiver_phone = #{receiverPhone},
            country = #{country},
            province = #{province},
            city = #{city},
            district = #{district},
            detail_address = #{detailAddress},
            postal_code = #{postalCode},
            is_default = #{isDefault},
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE tb_shipping_address
        SET deleted = 1,
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="cancelDefaultByUserId">
        UPDATE tb_shipping_address
        SET is_default = 0,
            update_time = NOW()
        WHERE user_id = #{userId}
          AND deleted = 0
    </update>

    <update id="setDefault">
        UPDATE tb_shipping_address
        SET is_default = 1,
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

</mapper>
