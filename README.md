# 跨境日用小商品订货系统 (CrossBorder Shop)

## 项目概述

跨境日用小商品订货系统是一个完整的电商解决方案，包含管理端和用户端两个独立的前端应用。

### 项目结构

```
CrossBorder-Shop/
├── .shop/              # 管理端前端 (Admin Frontend)
├── user/               # 用户端前端 (User Frontend)
├── backend/            # 后端服务
├── .backend/           # 后端配置
└── default_OpenAPI.json # API 文档
```

## 🎯 功能特性

### 管理端 (.shop)

**用户管理**
- ✅ JWT 认证登录/登出
- ✅ 用户个人资料管理
- ✅ 密码修改

**商品管理**
- ✅ 商品列表 (搜索、筛选、分页)
- ✅ 创建/编辑/删除商品
- ✅ 商品图片上传
- ✅ 商品状态管理 (上架/下架)
- ✅ 库存管理

**分类管理**
- ✅ 树形分类结构
- ✅ 创建/编辑/删除分类
- ✅ 父子分类关联

**订单管理**
- ✅ 订单列表 (多条件筛选)
- ✅ 订单详情
- ✅ 订单状态更新 (发货、取消、完成)
- ✅ 订单时间轴
- ✅ 退款处理

**地址管理**
- ✅ 收货地址 CRUD
- ✅ 默认地址设置

**仪表盘**
- ✅ 销售统计
- ✅ 订单统计
- ✅ 商品统计
- ✅ 用户统计

### 用户端 (user)

**首页**
- ✅ 轮播图
- ✅ 热销推荐
- ✅ 分类导航
- ✅ 新品展示

**商品浏览**
- ✅ 商品列表 (网格/列表视图)
- ✅ 分类筛选
- ✅ 搜索功能
- ✅ 价格排序
- ✅ 商品详情页
- ✅ 图片画廊

**购物车**
- ✅ 添加/删除商品
- ✅ 数量调整
- ✅ 价格计算
- ✅ 本地存储

**用户中心**
- ✅ 用户注册/登录
- ✅ 个人资料
- ✅ 订单历史
- ✅ 订单详情
- ✅ 地址管理
- ✅ 收藏夹
- ✅ 账户设置

**结算流程**
- ✅ 地址选择/新建
- ✅ 订单确认
- ✅ 价格计算
- ✅ 下单功能

## 🛠️ 技术栈

### 前端框架
- **Umi 4** - React 应用框架
- **React 18** - UI 库
- **TypeScript 5** - 类型安全

### UI 组件
- **Ant Design 5** - UI 组件库
- **Ant Design Pro Components** - 高级组件 (管理端)
- **CSS Modules** - 样式方案

### 状态管理 & 工具
- **ahooks** - React Hooks 库
- **axios** - HTTP 客户端
- **lodash** - 工具函数
- **luxon** - 日期处理

### 开发工具
- **pnpm** - 包管理器
- **ESLint** - 代码检查
- **Prettier** - 代码格式化

## 📦 安装与运行

### 前置要求

- Node.js >= 16
- pnpm >= 8

### 安装依赖

```bash
# 管理端
cd .shop
pnpm install

# 用户端
cd user
pnpm install
```

### 环境配置

复制 `.env.example` 为 `.env` 并配置:

```bash
# .shop/.env 和 user/.env
REACT_APP_API_BASE_URL=http://localhost:8080/api
NODE_ENV=development
```

### 启动开发服务器

```bash
# 管理端 (默认: http://localhost:8000)
cd .shop
pnpm dev

# 用户端 (默认: http://localhost:8001)
cd user
pnpm dev
```

### 构建生产版本

```bash
# 管理端
cd .shop
pnpm build

# 用户端
cd user
pnpm build
```

## 📚 项目文档

### 管理端
- [管理端 README](.shop/README.md) - 详细开发文档

### 用户端
- [用户端 README](user/README.md) - 详细开发文档
- [实现总结](user/IMPLEMENTATION_SUMMARY.md) - 功能实现详情
- [完成总结](user/PROJECT_COMPLETION_SUMMARY.md) - 项目完成报告

### API 文档
- [OpenAPI 规范](default_OpenAPI .json) - 后端 API 文档

## 🔐 认证机制

### JWT Token 认证
- 登录成功后获取 JWT Token
- Token 存储在 localStorage
- 请求自动携带 Authorization header
- Token 过期自动跳转登录页

### 权限控制
- 管理端: 管理员权限
- 用户端: 普通用户权限

## 🎨 设计规范

### 命名规范
- **文件夹**: kebab-case (如: `user-management`)
- **组件/页面**: PascalCase (如: `UserManagement.tsx`)
- **变量/函数**: camelCase (如: `getUserList`)
- **常量**: UPPER_SNAKE_CASE (如: `API_BASE_URL`)

### 代码规范
- TypeScript 严格模式
- 禁止使用 `any` 类型
- ESLint + Prettier 格式化
- 组件 Props 必须定义接口

## 📱 响应式设计

### 断点
- **手机**: < 768px
- **平板**: 768px - 992px
- **桌面**: > 992px

### 移动端适配
- 用户端: 底部导航栏
- 管理端: 侧边栏折叠
- 触摸友好的交互区域

## 🔍 目录结构

### 管理端 (.shop)
```
.shop/
├── src/
│   ├── pages/           # 页面组件
│   ├── layouts/         # 布局组件
│   ├── components/      # 公共组件
│   ├── services/        # API 服务层
│   ├── models/          # 数据模型
│   ├── utils/           # 工具函数
│   └── assets/          # 静态资源
├── public/              # 公共资源
├── .umirc.ts           # Umi 配置
└── package.json
```

### 用户端 (user)
```
user/
├── src/
│   ├── pages/           # 页面组件
│   ├── layouts/         # 布局组件
│   ├── components/      # 公共组件
│   ├── services/        # API 服务层
│   ├── models/          # 数据模型
│   ├── utils/           # 工具函数
│   └── assets/          # 静态资源
├── public/              # 公共资源
├── .umirc.ts           # Umi 配置
└── package.json
```

## 🚀 部署

### 构建
```bash
# 管理端
cd .shop && pnpm build

# 用户端
cd user && pnpm build
```

### 部署目录
- 管理端: `.shop/dist/`
- 用户端: `user/dist/`

### Nginx 配置示例

```nginx
# 管理端
server {
    listen 80;
    server_name admin.example.com;
    root /path/to/.shop/dist;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# 用户端
server {
    listen 80;
    server_name shop.example.com;
    root /path/to/user/dist;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 🤝 贡献指南

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

Apache 2.0 License

## 👥 团队

CrossBorder 开发团队

## 📧 联系方式

- 网站: https://www.crossborder.shop
- 邮箱: dev@crossborder.shop

## 🎉 致谢

感谢所有为这个项目做出贡献的开发者！
